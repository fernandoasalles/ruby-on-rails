<% if notice %>
  <div class="aviso"><%= notice %></div>
<% end %>

<% content_for :title, t('.title') %>

<div style="text-align: right; padding: 8px;">
  <%= link_to "🇧🇷 Português", url_for(locale: 'pt-BR') %> |
  <%= link_to "🇺🇸 English", url_for(locale: 'en') %>
</div>

<h1><%= t('.title') %></h1>

<div id="receitas">
  <% @receitas.each do |receita| %>
    <%= render receita %>
    <p>
      <%= button_to t('.show'), receita, method: :get, class: "botao ver" %>
    </p>
  <% end %>
</div>

<%= button_to t('.new'), new_receita_path, method: :get, class: "botao criar" %>
